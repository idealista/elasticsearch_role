---
## General
__elasticsearch_version: 8.7.1
__elasticsearch_major_version: "{{ elasticsearch_version.split('.')[0] }}"

## Service
# Start on boot
__elasticsearch_service_enabled: true

# Current state: started, stopped
__elasticsearch_service_state: started
